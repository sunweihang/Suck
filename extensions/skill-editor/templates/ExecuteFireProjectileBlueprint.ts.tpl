    private {{METHOD_NAME}}(): void {
        this.fireProjectile(
            {{INNER_prefab}},
            {{IN_0}} as Node | null,
            {{INNER_yOffset}},
            {{IN_1}},
            {{IN_2}}
        );
{{FLOW_0}}
    }
