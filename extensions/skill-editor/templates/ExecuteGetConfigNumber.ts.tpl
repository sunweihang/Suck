    private {{METHOD_NAME}}(): void {
        const key = {{INNER_key}};
        const defaultValue = {{INNER_defaultValue}};
        {{OUT_0}} = this.getConfigNumber(key, defaultValue);
{{FLOW_0}}
    }
