    private {{METHOD_NAME}}(): void {
        {{OUT_0}} = this.getEntityBoneWorldPosition(
            {{IN_0}} as Node | null,
            {{INNER_bone}},
            new Vec3({{INNER_localOffsetX}}, {{INNER_localOffsetY}}, {{INNER_localOffsetZ}})
        );
{{FLOW_0}}
    }
